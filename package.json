{
  "name": "sinau-tailwind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss build src/tailwind.css -o dist/style.css",
    "watch:tailwind": "nodemon --watch src/tailwind.css -x \"npm run build:css\"",
    "server": "npm run watch:tailwind",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "tailwindcss": "^2.0.2"
  }
}